# Automated update log

The scheduled updater appends one line for each completed source check. Failed
or incomplete updates never replace the published `docs/` output; their
details remain in the corresponding GitHub Actions run.
