# Automated update log

The scheduled updater appends one line here only after a source change has
been detected and a new output has passed validation. Failed or incomplete
updates never replace the published `docs/` output; their details remain in
the corresponding GitHub Actions run.
